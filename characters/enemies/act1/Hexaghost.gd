class_name Hexaghost extends Character

#TODO: hexaghost needs extra logic for body+orbs

#func load_character(node):
	#node.load_character("images/monsters/theBottom/sentry/skeleton.atlas", 
						#"images/monsters/theBottom/sentry/skeleton.json", 
						#2)
	#node.load_animation("idle",true,2.0)
	#node.set_mix("idle","attack",0.1)
	#node.set_mix("idle","spaz1",0.1)
	#node.set_mix("idle","hit",0.1)
	
